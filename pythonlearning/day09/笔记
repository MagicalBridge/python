1.join  等待所有的子线程执行完毕之后 主线程接着走
2.守护进程
    你是主人 你找来了许多仆人  仆人为你服务
    前提是有你，如果你没了  那么守护进程也就没了
3.不加join的时候主线程和子线程是完全并行执行的
  没有依赖关系 加上join之后主线程和子线程有了依赖关系
4.我将所有的子线程变成守护线程
  只要主线程执行完毕，它不会等待子线程执行完毕
  而是只要非守护线程执行完毕 程序就退出了 程序就不会管
  守护线程了
 5.四核的cpu 同一时刻  的的确确就是有四个人物在执行
   这就是多核的意义，在python 中  无论你有多少核 永远都是假象
   同一时间执行的线程只有一个 这是python的一个缺陷

   python 的线程是调用原生的系统的线程
